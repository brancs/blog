import { IconProps, IconifyIcon } from "@iconify/react/dist/iconify.js"
import { ElementType } from "react"

export type PostType = {
  id: string
  icon: string
  title: string
  description: string
}